<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  WaxFlow — one-way NAS -> Mac library replication (replaces Synology Drive).

  Install ON the Lexicon host Mac. launchd does NOT expand ~, so the absolute
  path below must be rewritten for your user:

    mkdir -p ~/.waxflow ~/Library/LaunchAgents
    cp scripts/sync-nas-to-mac.sh scripts/ensure-music-mount.sh ~/.waxflow/
    chmod +x ~/.waxflow/*.sh
    sed "s|/Users/willcurran|$HOME|g" scripts/com.waxflow.sync-database.plist \
        > ~/Library/LaunchAgents/com.waxflow.sync-database.plist
    launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.waxflow.sync-database.plist

  Then create ~/.waxflow/waxflow.conf with your NAS address:
    WAXFLOW_SHARE_HOST=192.168.1.50
    WAXFLOW_NAS_SSH=nas            # optional, enables ~1s incremental passes

  macOS TCC will deny a launchd agent access to /Volumes network mounts until you
  add /bin/bash under System Settings > Privacy & Security > Full Disk Access.

  120 s matches com.waxflow.mount-music, so a post-sleep remount is healed before
  the next replication pass. The script self-locks, so a long first pass simply
  causes subsequent firings to exit immediately.
-->
<plist version="1.0">
<dict>
  <key>Label</key><string>com.waxflow.sync-database</string>
  <key>ProgramArguments</key>
  <array>
    <string>/bin/bash</string>
    <string>/Users/willcurran/.waxflow/sync-nas-to-mac.sh</string>
  </array>
  <key>RunAtLoad</key><true/>
  <key>StartInterval</key><integer>120</integer>
  <key>StandardOutPath</key><string>/Users/willcurran/.waxflow/sync-database.launchd.out.log</string>
  <key>StandardErrorPath</key><string>/Users/willcurran/.waxflow/sync-database.launchd.err.log</string>
  <key>LowPriorityIO</key><true/>
  <key>Nice</key><integer>5</integer>
</dict>
</plist>
